# Core parsing
lark-parser==0.12.0

# Numerical computations for SR / weight profiling
numpy==1.26.4

# Interactive CLI enhancements (optional but recommended)
rich==13.7.0
prompt-toolkit==3.0.43

# For JSON/file operations (already standard)
# json, pathlib are standard – no need to include

# Testing (for pytest-based tests)
pytest==8.2.2
